"use strict";

const prompt = require("prompt");
const { getAllProducts, postOnProduct, deleteOneProduct } = require("../models/Product.js");


module.exports.promptDeleteOneProduct = () => {
    return new Promise( (resolve, reject) => {

    })
}